{
  "_comment": "Vite example app — depends on workspace package dot-js.",
  "name": "example",
  "version": "0.1.0",
  "private": true,
  "description": "Sample app that exercises dot-js (dev server via Vite).",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "dot-js": "file:../framework"
  },
  "devDependencies": {
    "vite": "^6.2.0"
  }
}
